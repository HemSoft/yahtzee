# Convex deployment URL — run `bun x convex dev` to get this value
# Used by web (Vite) and desktop (Electron/electron-vite)
VITE_CONVEX_URL=

# Used by mobile (Expo) — same URL, different env var prefix
EXPO_PUBLIC_CONVEX_URL=
